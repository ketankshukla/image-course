# Diagram 220 — Capstone: Acme Agent Workspace and Learning Platform

![The Acme capstone connects an agent workspace and visual learning platform through human-control and learning loops, using Next.js, React, FastAPI, MCP, A2A, AG-UI, MCP Apps, A2UI, RAG, and tools under shared accessibility, security, privacy, observability, evaluation, and offline controls.](../diagrams/220-acme-agent-workspace-learning-platform.png)

**Module:** Agentic product operations
**Role in the course:** Combine the full visual course into an implementation-ready product specification without prematurely writing the Next.js or Python project.
**Layout:** The diagram shows two connected product zones AGENT WORKSPACE and VISUAL LEARNING PLATFORM, with a coral risk path.

---

## At a glance

**Combine the full visual course into an implementation-ready product specification without prematurely writing the Next.js or Python project.**

- The diagram centers on **AGENT WORKSPACE** and its relationship to **STALE POLICY UNSAFE UI EXPIRED APPROVAL DATA LEAK**.

- The diagram separates the tested, legitimate flow from failure paths that must fail closed.

- Maya's case: Maya reviews an urgent refund exception while the current policy conflicts with a stale retrieved copy and a third-party app proposes an unsafe custom approval surface.

---

## What the diagram teaches

### 1. Capstone Is One Coherent Product With Two Experiences

The capstone is one coherent product with two experiences. The diagram makes this concrete through **AGENT WORKSPACE**, **VISUAL LEARNING PLATFORM**, **NEXTJS REACT FASTAPI MCP A2A AG-UI MCP APPS A2UI RAG TOOLS**. If the team skips this, a demo can appear full-stack while hiding the exact boundaries that matter: authoritative state, protocol versions, identity, policy, consent, accessibility, data deletion, and recovery after uncertain effects. This is the lesson the case study ends with: Build one observable human-control system and two understandable experiences; protocols connect parts, but product contracts make them safe and teachable.

### 2. A2A Coordinates Task-oriented Peer Agents

A2A coordinates task-oriented peer agents. The same structured content model produces the document, website, search, and offline pack. This is visible in the drawing as **AGENT WORKSPACE**, **VISUAL LEARNING PLATFORM**, **NEXTJS REACT FASTAPI MCP A2A AG-UI MCP APPS A2UI RAG TOOLS**. Without this step, a demo can appear full-stack while hiding the exact boundaries that matter: authoritative state, protocol versions, identity, policy, consent, accessibility, data deletion, and recovery after uncertain effects. In the walkthrough, The evidence freshness rule marks the stale copy as superseded and blocks the proposal from becoming actionable..

### 3. Maya Opens A Customer Case And Requests A Refund-policy Review

This step asks the team to maya opens a customer case and requests a refund-policy review with a named outcome, constraints, and privacy scope. The diagram shows this through **CASE**, **ACCESSIBILITY SECURITY PRIVACY OBSERVABILITY EVALUATION OFFLINE**, **STALE POLICY UNSAFE UI EXPIRED APPROVAL DATA LEAK**, which make the abstract step visible and testable. FastAPI owns typed APIs, durable work coordination, authorization, policy, consent, notifications, content validation, and export or deletion workflows. None of these protocols replaces product identity, authorization, policy, or receipts. If the team skips this, a demo can appear full-stack while hiding the exact boundaries that matter: authoritative state, protocol versions, identity, policy, consent, accessibility, data deletion, and recovery after uncertain effects. Maya's case makes this concrete: Maya reviews an urgent refund exception while the current policy conflicts with a stale retrieved copy and a third-party app proposes an unsafe custom approval surface.
![User outcomes connect to minimized product events, feedback, quality evaluation, safety review, and accessibility evidence; every metric exposes its definition and denominator, while experiments require consent, risk review, fair allocation, stop rules, guardrails, and debriefing.](../diagrams/219-analytics-feedback-evaluation-experiment-ethics.png)

Diagram 219 — *Product analytics, feedback, evaluation, and experiment ethics* is the next lens on this idea. The same concepts reappear there with different emphasis, so the two diagrams should be read as a pair.

### 4. Coordinator Delegates Bounded Research Through A2A, Reaches Approved Evidence

Here the product must coordinator delegates bounded research through A2A, reaches approved evidence and tools through MCP, and streams typed AG-UI state into the workspace. In the drawing, **EVIDENCE**, **NEXTJS REACT FASTAPI MCP A2A AG-UI MCP APPS A2UI RAG TOOLS**, **AGENT WORKSPACE** carry this responsibility. MCP connects agents to declared tools, resources, prompts, and optional Apps. AG-UI carries typed interface events. Without this step, a demo can appear full-stack while hiding the exact boundaries that matter: authoritative state, protocol versions, identity, policy, consent, accessibility, data deletion, and recovery after uncertain effects. The result — The system completes useful work while stale evidence and unsafe interface code fail closed without erasing progress or confusing Maya. — depends on getting this right.

### 5. RAG Returns Versioned Evidence; An A2UI Or MCP App Surface

The diagram enforces this by showing the team how to rAG returns versioned evidence; an A2UI or MCP App surface may present an approved component view without receiving unrestricted code or host access. The visual anchors are **EVIDENCE**, **NEXTJS REACT FASTAPI MCP A2A AG-UI MCP APPS A2UI RAG TOOLS**; without them the step would be invisible to the user. Next.js and React own the web shell, accessible views, event reducer, diagrams, embedded app host, local learning state, and user controls. RAG grounds answers in versioned evidence. The case study shows the risk: a demo can appear full-stack while hiding the exact boundaries that matter: authoritative state, protocol versions, identity, policy, consent, accessibility, data deletion, and recovery after uncertain effects. This is the lesson the case study ends with: Build one observable human-control system and two understandable experiences; protocols connect parts, but product contracts make them safe and teachable.

### 6. Acme Creates A Proposal Bound To Current Evidence, Policy, Amount

This is the discipline that makes the product acme creates a proposal bound to current evidence, policy, amount, customer, actor, expiry, and recovery; Maya reviews and decides accessibly. This idea sits on **EVIDENCE** and reaches the rest of the diagram through **EVIDENCE**, **PROPOSAL**, **RECOVERY**. The Agent Workspace helps Maya complete governed customer work; the Visual Learning Platform teaches her how the protocols, evidence, decisions, and controls operate through the same visual course structure. A2UI can describe catalog-bound interface structure. Missing this is how products end up with a demo can appear full-stack while hiding the exact boundaries that matter: authoritative state, protocol versions, identity, policy, consent, accessibility, data deletion, and recovery after uncertain effects. In the walkthrough, The evidence freshness rule marks the stale copy as superseded and blocks the proposal from becoming actionable..

### 7. Authoritative Action Returns A Receipt, Partial Or Failed Work Remains

The team must authoritative action returns a receipt, partial or failed work remains visible, evaluation and privacy-minimized analytics record the outcome, and related visual lessons explain every layer before the interface can be trustworthy. The diagram shows this through **ACTION**, **RECEIPT**, **VISUAL**, which make the abstract step visible and testable. The human-control loop is request, visible plan, honest progress, evidence, bounded proposal, accessible decision, authoritative action, receipt, and recovery. A system that ignores this will eventually face a demo can appear full-stack while hiding the exact boundaries that matter: authoritative state, protocol versions, identity, policy, consent, accessibility, data deletion, and recovery after uncertain effects. The danger the case warns about, Maya reviews an urgent refund exception while the current policy conflicts with a stale retrieved copy and a third-party app proposes an unsafe custom approval surface. should make this clear.

### 8. The standard in practice

The lesson is not a perfect prototype; it is a product contract. Combine the full visual course into an implementation-ready product specification without prematurely writing the Next.js or Python project.. The diagram makes that contract visible through **AGENT WORKSPACE**, **VISUAL LEARNING PLATFORM**, **NEXTJS REACT FASTAPI MCP A2A AG-UI MCP APPS A2UI RAG TOOLS**, so the team can argue about the contract before choosing a framework. The contract is not framework-specific; it is the set of observable promises the product makes to the person using it. Maya's case warns that a demo can appear full-stack while hiding the exact boundaries that matter: authoritative state, protocol versions, identity, policy, consent, accessibility, data deletion, and recovery after uncertain effects. The practical standard is this: Build one observable human-control system and two understandable experiences; protocols connect parts, but product contracts make them safe and teachable.

### 9. The Next.js and Python surfaces

The same contract appears in both stacks, but the boundary moves with the architecture.
**In Next.js / React:**
- Create route groups for workspace, tasks, artifacts, approvals, learning, search, privacy, and settings; keep privileged data server-side and isolate live or device behavior in narrow client components.
- Implement one typed product-event adapter and reducer, one approved generated-component catalog, one sandboxed MCP App host, and one semantic visual-lesson renderer.
- Build accessible state fixtures for every stage, partial success, conflict, approval, expiry, recovery, offline mode, privacy choice, and security block before connecting live services.
- Keep the most sensitive payloads and policy decisions on the server and stream only user-visible, safe view models to client components.
**In Python / FastAPI:**
- Define Pydantic contracts for tasks, events, evidence, proposals, decisions, tool intents, artifacts, receipts, lessons, consent, memory, notifications, feedback, and deletions.
- Separate authoritative business records from event projections; coordinate durable work and compensation while adapting MCP, A2A, AG-UI, and A2UI versions at explicit boundaries.
- Ship deterministic simulators and end-to-end fixtures first, then add providers behind interfaces with contract, policy, security, replay, accessibility-state, and evaluation tests.
- Treat every framework callback or protocol event as raw input; validate and transform it into the product's own event vocabulary before it reaches the reducer or domain model.
Both implementations must avoid the same trap: a demo can appear full-stack while hiding the exact boundaries that matter: authoritative state, protocol versions, identity, policy, consent, accessibility, data deletion, and recovery after uncertain effects.

### 10. Analogy

An airport combines flight operations and a passenger information system. The engines, control tower, gates, signs, staff, security, accessibility services, and training must share truth without giving every part the same authority. The analogy keeps the lesson grounded. The diagram's **AGENT WORKSPACE** plays the same role as the central object in the analogy: it must be visible, labeled, and trustworthy without the user having to infer meaning from a long narrative. The parallel works because both situations require separating the object from the record, the attempt from the proof, and the promise from the evidence.

---

## Case study — Maya

Maya reviews an urgent refund exception while the current policy conflicts with a stale retrieved copy and a third-party app proposes an unsafe custom approval surface.

### The walkthrough

1. The evidence freshness rule marks the stale copy as superseded and blocks the proposal from becoming actionable.
2. The generated UI validator rejects the unapproved component and the sandboxed app cannot call a host approval method without capability, consent, and server policy.
3. Acme preserves completed research, retrieves the current policy, replans the comparison, and presents a native accessible approval card bound to the new evidence version.
4. Maya approves the bounded exception; the server commits once, returns a receipt, updates the task, and links the relevant security, RAG, approval, and recovery lessons.

### The result

The system completes useful work while stale evidence and unsafe interface code fail closed without erasing progress or confusing Maya.

### The danger

A demo can appear full-stack while hiding the exact boundaries that matter: authoritative state, protocol versions, identity, policy, consent, accessibility, data deletion, and recovery after uncertain effects.

### The takeaway

Build one observable human-control system and two understandable experiences; protocols connect parts, but product contracts make them safe and teachable.

---

## Composition

The picture is a single-view explainer for *Capstone: Acme Agent Workspace and Learning Platform*. On the left, the diagram shows two connected product zones AGENT WORKSPACE and VISUAL LEARNING PLATFORM. At the top, inputs NEXTJS REACT FASTAPI MCP A2A AG-UI MCP APPS A2UI RAG TOOLS. In the center, center HUMAN CONTROL LOOP REQUEST, PLAN, PROGRESS, EVIDENCE, PROPOSAL, APPROVAL, ACTION, RECEIPT, RECOVERY. To the right, learning loop VISUAL, EXPLANATION, CASE, LAB, CHECKPOINT, PROGRESS. Across the middle, cross-cutting ACCESSIBILITY SECURITY PRIVACY OBSERVABILITY EVALUATION OFFLINE. The eye travels from **AGENT WORKSPACE** through the central flow to **STALE POLICY UNSAFE UI EXPIRED APPROVAL DATA LEAK**, with the contrast between coral risk paths and teal recovery paths giving the reader the core message at a glance.

## Element by element

- **AGENT WORKSPACE** — the product where Maya completes governed customer work.
- **VISUAL LEARNING PLATFORM** — the product that teaches the course through the same visual model.
- **NEXTJS REACT FASTAPI MCP A2A AG-UI MCP APPS A2UI RAG TOOLS** — the nextjs react fastapi mcp a2a ag-ui mcp apps a2ui rag tools Inputs NEXTJS REACT FASTAPI MCP A2A AG-UI MCP APPS A2UI RAG TOOLS..
- **HUMAN CONTROL LOOP REQUEST** — the human control loop request Center HUMAN CONTROL LOOP REQUEST, PLAN, PROGRESS, EVIDENCE, PROPOSAL, APPROVAL, ACTION, RECEIPT, RECOVERY..
- **PLAN** — the visible set of steps, constraints, and priorities for completing the request.
- **PROGRESS** — the honest, observable state of the running task.
- **EVIDENCE** — versioned support for a decision, with source, freshness, and authority.
- **PROPOSAL** — the immutable, versioned description of a requested decision.
- **APPROVAL** — one of the items named by **HUMAN CONTROL**; this is the **APPROVAL** item.
- **ACTION** — the committed, authoritative effect after a decision.
- **RECEIPT** — durable proof of a decision, effect, or user-visible transition.
- **RECOVERY** — the safe path after an error, conflict, partial success, or cancelled effect.
- **VISUAL** — one of the items named by **AGENT WORKSPACE**; this is the **VISUAL** item.
- **EXPLANATION** — the explanation Learning loop VISUAL, EXPLANATION, CASE, LAB, CHECKPOINT, PROGRESS..
- **CASE** — the case Learning loop VISUAL, EXPLANATION, CASE, LAB, CHECKPOINT, PROGRESS..
- **LAB** — the lab Learning loop VISUAL, EXPLANATION, CASE, LAB, CHECKPOINT, PROGRESS..
- **CHECKPOINT** — the checkpoint Learning loop VISUAL, EXPLANATION, CASE, LAB, CHECKPOINT, PROGRESS..
- **ACCESSIBILITY SECURITY PRIVACY OBSERVABILITY EVALUATION OFFLINE** — the accessibility security privacy observability evaluation offline Cross-cutting ACCESSIBILITY SECURITY PRIVACY OBSERVABILITY EVALUATION OFFLINE..
- **STALE POLICY UNSAFE UI EXPIRED APPROVAL DATA LEAK** — the stale policy unsafe ui expired approval data leak blocked..

---

## Colour and flow semantics

The course visual grammar applies directly to this diagram.

- **Cobalt platform** — A browser surface, state store, component catalog, product boundary, workspace, or learning-platform region. Here the cobalt surface under **AGENT WORKSPACE** and the surrounding workspace frames the product boundary; it tells the reader that this is a bounded product region, not an uncontrolled chat surface. It marks the product's responsibility to keep the user's workspace distinct from raw model output or runtime internals.

- **Cyan arrow** — A typed event, validated state update, user action, artifact reference, notification, or navigation path. Cyan arrows carry the flow of events, updates, and proposals from left to right, linking the typed cards and records to the reducer or next gate. When the reader sees cyan, they should expect a deliberate, validated transition rather than an accidental or hidden connection.

- **Teal arrow** — Authoritative state, accessible completion, informed consent, safe approval, preserved work, or verified recovery. Teal marks the authoritative and safe path, such as the committed receipt or restored state, distinguishing it from the raw request. This color tells the reader that the product has enough evidence and authority to proceed safely.

- **Coral path** — Conflict, stale UI, unsafe rendering, deceptive state, expired approval, privacy failure, lost work, or blocked action. Coral marks the conflict, stale state, or blocked action that appears when a control is missing or bypassed. It signals that the product should pause, surface the conflict, and offer a recovery path rather than proceed silently.

- **White card** — An event, component, stage, proposal, artifact, receipt, consent record, lesson, checkpoint, or recovery option. **PROPOSAL**, **RECEIPT** appear as white cards, showing that they are records, proposals, or evidence rather than raw data or system internals. The white background separates user-meaningful records from raw system logs or generated text.

The overall flow moves from the initiating actor or request, through validation and reduction, toward either the teal recovery or completion path or the coral failure path. The color of each arrow and card is a trust cue, not a decoration.

---

## How to present it

- **Start with the user moment.** Maya reviews an urgent refund exception while the current policy conflicts with a stale retrieved copy and a third-party app proposes an unsafe custom approval surface. Ask the room what they need to see to feel in control. Invite someone to describe the moment before any solution appears.

- **Point at AGENT WORKSPACE and ask what it represents.** Then trace the flow from there through the next two or three elements, naming the color and direction of each arrow. Encourage them to name the incoming trigger, the visible state, and the decision the product must make.

- **Point at CASE for step 1.** Maya Opens A Customer Case And Requests A Refund-policy Review. Ask what observable evidence the product would produce if this step worked correctly. Have the room identify the color of the path leaving this element and what it tells a user about trust.

- **Point at EVIDENCE for step 2.** Coordinator Delegates Bounded Research Through A2A, Reaches Approved Evidence. Ask what observable evidence the product would produce if this step worked correctly. Have the room identify the color of the path leaving this element and what it tells a user about trust.

- **Point at EVIDENCE for step 3.** RAG Returns Versioned Evidence; An A2UI Or MCP App Surface. Ask what observable evidence the product would produce if this step worked correctly. Have the room identify the color of the path leaving this element and what it tells a user about trust.

- **Point at EVIDENCE for step 4.** Acme Creates A Proposal Bound To Current Evidence, Policy, Amount. Ask what observable evidence the product would produce if this step worked correctly. Have the room identify the color of the path leaving this element and what it tells a user about trust.

- **Point at ACTION for step 5.** Authoritative Action Returns A Receipt, Partial Or Failed Work Remains. Ask what observable evidence the product would produce if this step worked correctly. Have the room identify the color of the path leaving this element and what it tells a user about trust.

- **Use the analogy.** An airport combines flight operations and a passenger information system. The engines, control tower, gates, signs, staff, security, accessibility services, and training must share truth without giving every part the same authority. Draw the parallel to the diagram and ask what would break if the two situations were handled the same way.

- **Tell the case study.** Maya reviews an urgent refund exception while the current policy conflicts with a stale retrieved copy and a third-party app proposes an unsafe custom approval surface Walk through the walkthrough and stop at the moment the old design would have failed. Pause at the step where the old design would have failed and ask how the new interface prevents it.

- **Run the lab.** Write the capstone design package: context and trust diagrams, domain and event models, task states and sequences, component and approval contracts, APIs, storage and deletion, accessibility, threats, evaluation, notifications, offline behavior, deployment, thirty acceptance tests, and a phased Next.js plus FastAPI backlog. Do not write production code yet. Ask pairs to present one part of the diagram and explain how the other parts reference it without merging their identities.

- **Pose the checkpoint.** Which protocol is the source of truth for whether the refund actually committed? Let the room answer before confirming the principle. Collect answers, then confirm the principle and note any exceptions the team should watch for.

- **Close on the contract.** Build one observable human-control system and two understandable experiences; protocols connect parts, but product contracts make them safe and teachable. Ask the team what their own product's equivalent of the contract would be.

---

## Lab and checkpoint

**Lab:** Write the capstone design package: context and trust diagrams, domain and event models, task states and sequences, component and approval contracts, APIs, storage and deletion, accessibility, threats, evaluation, notifications, offline behavior, deployment, thirty acceptance tests, and a phased Next.js plus FastAPI backlog. Do not write production code yet.

**Checkpoint:** Which protocol is the source of truth for whether the refund actually committed?

**Answer:** None by itself. The authoritative Acme business system and its idempotent receipt establish the committed effect. MCP, A2A, AG-UI, and A2UI carry tools, tasks, interface events, or views around that truth.

---

## Glossary

- **Human-control loop** — visible sequence from request through decision, effect, receipt, and recovery
- **Product contract** — owned rule for state, authority, data, and behavior
- **Capstone** — integrated design demonstrating the complete course

---

## Sources

- [MCP 2026-07-28 specification](https://modelcontextprotocol.io/specification/2026-07-28)
- [MCP Apps stable specification](https://apps.extensions.modelcontextprotocol.io/)
- [A2UI specification versions](https://a2ui.org/)
- [AG-UI overview](https://docs.ag-ui.com/)
- [WCAG 2.2](https://www.w3.org/TR/WCAG22/)
- [NIST AI Risk Management Framework](https://www.nist.gov/itl/ai-risk-management-framework)

## Related lessons

- Diagram 197 — The event-driven interface mental model
- Diagram 205 — Interrupt, input request, approval, rejection, and expiry
- Diagram 213 — A reusable visual lesson content model
- Diagram 218 — Privacy controls, consent, memory settings, and deletion
- Diagram 219 — Product analytics, feedback, evaluation, and experiment ethics

---